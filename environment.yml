name: geo_env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - geopandas
  - rasterio
  - numpy
  - matplotlib
  - scipy
  - networkx
  - requests
  - osmnx 
  - pip
  - pip:
      - hda
      - python-dotenv
      - tqdm
      - elevation
      - flask